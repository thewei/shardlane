# ui-acceptance-testing/
> L2 | 父级: ../CLAUDE.md

成员清单

- `SKILL.md` — 模型可发现的 UI 验收主流程；默认通过 `mcp__node_repl__js` 调用 `@oai/sky`，原生 CGEvent 仅显式授权后使用。
- `references/computer-use-mcp.md` — Computer Use MCP 调用模板、manifest 隔离边界与风险确认。
- `references/evidence-schema.md` — `evidence.jsonl` 的 schema、字段边界和校验契约。
- `evals/evals.json` — 真实 UI/原生回退场景的技能行为评估。

法则: 驱动与断言分离·App 定向优先·不把截图当真值·控制面不写用户文本
[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md

