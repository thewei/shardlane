//! [INPUT]: Existing imports and types from the crate root (via the history module root glob: `use super::*` chain).
//! [OUTPUT]: For the crate::history family: transcript-window constants (the paging logic was removed).
//! [POS]: Windowing responsibility slice of the herdr-gui History surface; mechanically split out of history.rs.
